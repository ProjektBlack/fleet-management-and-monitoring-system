import React from "react";

const Spinner = (props) => {
    return (
        <div class="spinner-border text-success mx-auto d-flex" role="status" style={{ marginTop: "50vh" }}>
            <span class="sr-only"></span>
        </div>
    )
}

export default Spinner;